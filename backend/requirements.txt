# ═══════════════════════════════════════════════════════════════
# SecureAuth OS — backend/requirements.txt
#
# Install all dependencies with:
#   pip install -r requirements.txt
#
# Python version required: >= 3.10
# (uses the built-in X | Y union type hint syntax)
# ═══════════════════════════════════════════════════════════════

# ── Web framework ───────────────────────────────────────────────
Flask==3.0.3

# ── Cross-Origin Resource Sharing ───────────────────────────────
# Restricts API access to localhost origins only
Flask-Cors==4.0.1

# ── Rate limiting (brute-force / DoS protection) ────────────────
Flask-Limiter==3.7.0

# ── Password hashing ────────────────────────────────────────────
# bcrypt with cost factor 12 — industry standard for auth systems
bcrypt==4.1.3

# ── One-Time Password (TOTP) ────────────────────────────────────
# RFC-6238 compliant; used for 2FA step after credential check
pyotp==2.9.0

# ── WSGI server (production use) ────────────────────────────────
# Use instead of Flask's built-in dev server when deploying
# Run with: gunicorn -w 4 -b 127.0.0.1:5000 app:app
gunicorn==22.0.0
